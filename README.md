# Mohammed Hijazi Portfolio

Simple React + Vite setup for a personal portfolio. The home page currently shows your name so you can build out sections as you go.

## Getting started

- Install dependencies: `npm install`
- Run the dev server: `npm run dev`
- Create a production build: `npm run build`
- Preview the production build: `npm run preview`
